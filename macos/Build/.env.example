# Copy to .env or provide these values through the environment before release.sh.
SIGNING_IDENTITY="Developer ID Application: Your Name (TEAMID)"
NOTARY_PROFILE="FluxNews-notary"
# Optional: use only when the default Apple timestamp endpoint is unavailable.
# SIGNING_TIMESTAMP_URL="http://timestamp.example.invalid/ts01"
